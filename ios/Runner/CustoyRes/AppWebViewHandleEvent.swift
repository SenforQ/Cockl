
//: Declare String Begin

/*: "getDeviceID" :*/
fileprivate let viewPolicyDict:String = "us newgetDe"
fileprivate let engineMidstreamURL:String = "Inumber"

/*: "getFirebaseID" :*/
fileprivate let userMarginalDate:String = "getFiradjustment conduct"
fileprivate let controllerAdjustLocalValidTitle:String = "ebaseIDspace available secret"

/*: "getAreaISO" :*/
fileprivate let k_groundAllowKey:String = "pass"
fileprivate let mainInstallToken:String = "handle title that ensure productetAre"

/*: "getProxyStatus" :*/
fileprivate let dataCurfewTitle:String = "server currency kit bygetProxyS"
fileprivate let const_canValue:[Character] = ["t","a","t","u","s"]

/*: "getMicStatus" :*/
fileprivate let helperSecretMessage:String = "print info creategetMi"
fileprivate let notiAppealHourUrl:String = "cStatusm user protection re public"

/*: "getPhotoStatus" :*/
fileprivate let constMapVersion:[Character] = ["g","e","t","P","h","o","t","o","S","t","a"]
fileprivate let kAllowHandleSecret:String = "forwardus"

/*: "getCameraStatus" :*/
fileprivate let modelErrorPhoneToken:String = "getCamerus theory disappear"
fileprivate let data_applicationCenterPath:String = "ting blockaStatus"

/*: "reportAdjust" :*/
fileprivate let parserCaskMode:[Character] = ["r","e","p","o","r","t","A","d","j","u","s","t"]

/*: "requestLocalPush" :*/
fileprivate let const_mirrorTime:String = "original"
fileprivate let showPoorKey:String = "EQUES"
fileprivate let appSubUrl:String = "system agent adjustment piclPush"

/*: "getLangCode" :*/
fileprivate let k_behaviorMsg:String = "getLanvalue confirm return need global"
fileprivate let managerPromptTitle:String = "gCodeaccess color report"

/*: "getTimeZone" :*/
fileprivate let showBringAppearSecret:String = "getTimnative float hoi"
fileprivate let mainNameBackgroundTitle:String = "eZoneas ensure reduce"

/*: "getInstalledApps" :*/
fileprivate let managerSmartPath:String = "reduce privacy view loggetI"
fileprivate let cachePleaseMessage:[Character] = ["p","p","s"]

/*: "getSystemUUID" :*/
fileprivate let serviceHourSubVersion:[Character] = ["g","e","t","S","y","s"]
fileprivate let data_endBackPath:[Character] = ["t","e","m","U","U","I","D"]

/*: "getCountryCode" :*/
fileprivate let managerClickCount:String = "getCounminimum count after"
fileprivate let app_byURL:String = "tryCodetap already product black quality"

/*: "inAppRating" :*/
fileprivate let mainBarrelDate:String = "inAppRclear report"
fileprivate let networkAllowDict:String = "forward"

/*: "apPay" :*/
fileprivate let helperFrameState:String = "apPayafter access progress appeal"

/*: "subscribe" :*/
fileprivate let userTitleDocumentValue:String = "subscpoorib"
fileprivate let managerUnprocessedToken:String = "display"

/*: "openSystemBrowser" :*/
fileprivate let k_mirrorError:String = "openSlaunch challenge"
fileprivate let constNetworkID:String = "mBagain trigger"
fileprivate let routerContentTitle:[Character] = ["r","o","w","s","e","r"]

/*: "closeWebview" :*/
fileprivate let dataCancelDocumentFormat:[Character] = ["c","l","o","s","e","W","e","b","v","i","e","w"]

/*: "openNewWebview" :*/
fileprivate let sessionHeadName:String = "openNein never"
fileprivate let k_cutNamePath:[Character] = ["w","W","e","b","v","i","e","w"]

/*: "reloadWebview" :*/
fileprivate let dataTransformError:String = "reltoola"
fileprivate let sessionPathResult:String = "vframew"

/*: "openSettings" :*/
fileprivate let app_runKey:[Character] = ["o"]
fileprivate let formatterNativeYourUrl:[Character] = ["p","e","n","S","e","t","t","i","n","g","s"]

/*: "getNotificationStatus" :*/
fileprivate let routerTempResult:[Character] = ["g","e","t","N","o","t","i","f","i"]
fileprivate let constTransportMessage:[Character] = ["c","a","t","i","o","n","S","t","a","t","u","s"]

/*: "setScheduledLocalPush" :*/
fileprivate let serviceColorDict:[Character] = ["s","e","t","S","c","h","e"]
fileprivate let app_workingPapersString:[Character] = ["d","u","l","e","d","L","o","c","a","l","P","u","s","h"]

/*: "typeName" :*/
fileprivate let appPathDecisionTooPath:[UInt8] = [0xef,0xe2,0xeb,0xfe,0xd5,0xfa,0xf6,0xfe]

private func behaviorPresent(device num: UInt8) -> UInt8 {
    return num ^ 155
}

/*: "deviceID" :*/
fileprivate let engineLoadGlobalMessage:[UInt8] = [0xea,0xeb,0xf8,0xe7,0xed,0xeb,0xc7,0xca]

private func scalePanel(pad num: UInt8) -> UInt8 {
    return num ^ 142
}

/*: "fireBaseID" :*/
fileprivate let networkFileValue:[UInt8] = [0x4f,0x40,0x5b,0x4c,0x6b,0x48,0x5a,0x4c,0x60,0x6d]

/*: "areaISO" :*/
fileprivate let modelWorkerResult:[UInt8] = [0x4f,0x53,0x49,0x61,0x65,0x72,0x61]

/*: "isProxy" :*/
fileprivate let cacheHomePadDate:[UInt8] = [0xce,0xd4,0xf7,0xd5,0xc8,0xdf,0xde]

/*: "langCode" :*/
fileprivate let showPackageProgressMsg:[UInt8] = [0xdb,0xd0,0xdd,0xd6,0xb2,0xde,0xd3,0xd4]

fileprivate func minimumLevel(interval num: UInt8) -> UInt8 {
    let value = Int(num) - 111
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "timeZone" :*/
fileprivate let show_enterDefiniteValue:[UInt8] = [0x2d,0x30,0x34,0x3c,0x3,0x36,0x37,0x3c]

/*: "installedApps" :*/
fileprivate let kReceiptsVisibleDict:[UInt8] = [0xaf,0xa8,0xb5,0xb2,0xa7,0xaa,0xaa,0xa3,0xa2,0x87,0xb6,0xb6,0xb5]

private func blackHourResult(succeed num: UInt8) -> UInt8 {
    return num ^ 198
}

/*: "systemUUID" :*/
fileprivate let notiApplicationWaitId:[UInt8] = [0x6e,0x74,0x6e,0x6f,0x60,0x68,0x50,0x50,0x44,0x3f]

fileprivate func nativeAccess(format num: UInt8) -> UInt8 {
    let value = Int(num) + 5
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "countryCode" :*/
fileprivate let formatterCurrencyNetID:[UInt8] = [0xcf,0xc3,0xd9,0xc2,0xd8,0xde,0xd5,0xef,0xc3,0xc8,0xc9]

/*: "status" :*/
fileprivate let k_acrossToken:[UInt8] = [0x4e,0x4f,0x3c,0x4f,0x50,0x4e]

fileprivate func noPass(race num: UInt8) -> UInt8 {
    let value = Int(num) - 219
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isAuth" :*/
fileprivate let app_originTime:[UInt8] = [0xf7,0xed,0xdf,0xeb,0xea,0xf6]

/*: "isFirst" :*/
fileprivate let routerGatheringDate:[UInt8] = [0x3a,0x44,0x17,0x3a,0x43,0x44,0x45]

fileprivate func thirdCancel(phone num: UInt8) -> UInt8 {
    let value = Int(num) - 209
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: __LocalPush" :*/
fileprivate let viewDownTitle:[Character] = ["_","_","L","o","c","a","l","P","u","s","h"]

/*: "identifier" :*/
fileprivate let dataOrientList:[UInt8] = [0xf5,0xf8,0xf9,0xf2,0xe8,0xf5,0xfa,0xf5,0xf9,0xee]

private func requestTransform(ok num: UInt8) -> UInt8 {
    return num ^ 156
}

/*: "HTTPProxy" :*/
fileprivate let const_labelContentID:String = "htt"
fileprivate let mainHoiValue:String = "PProxyevent format scale short data"

/*: "HTTPSProxy" :*/
fileprivate let viewTodayMessage:String = "HindexindexPS"
fileprivate let helperAtResult:[Character] = ["P","r","o","x","y"]

/*: "SOCKSProxy" :*/
fileprivate let main_sLogToken:[Character] = ["S","O","C","K","S"]
fileprivate let controllerKindKey:String = "go globalProxy"

/*: "__SCOPED__" :*/
fileprivate let serviceLanguageCount:[Character] = ["_","_","S","C","O","P","E","D","_"]
fileprivate let formatterFormatServerError:String = "to"

/*: "tap" :*/
fileprivate let networkLevelValue:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let formatterActiveID:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let main_originalMessage:String = "clicksec"

/*: "ppp" :*/
fileprivate let data_panelMode:String = "alreadyalreadyalready"

/*: "Retry After or Go to " :*/
fileprivate let k_listChallengeMessage:[Character] = ["R","e","t","r","y"," ","A","f","t","e"]
fileprivate let cacheSaveStatus:String = "float listr or"

/*: "Feedback" :*/
fileprivate let user_applianceError:[Character] = ["F","e","e","d","b"]
fileprivate let constDomainMainValue:String = "acfail"

/*: " to contact us" :*/
fileprivate let configBondRatingId:[Character] = [" ","t","o"," ","c","o","n","t","a","c"]
fileprivate let helperImageProductID:[Character] = ["t"," ","u","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AppWebViewHandleEvent.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// H5事件
//: private let getDeviceID     = "getDeviceID"        // 获取设备号
private let parserTooValue     = (String(viewPolicyDict.suffix(5)) + "vice" + engineMidstreamURL.replacingOccurrences(of: "number", with: "D"))        // 获取设备号
//: private let getFirebaseID   = "getFirebaseID"      // 获取FireBaseToken
private let mainModeMode   = (String(userMarginalDate.prefix(6)) + String(controllerAdjustLocalValidTitle.prefix(7)))      // 获取FireBaseToken
//: private let getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
private let appRaceID      = (k_groundAllowKey.replacingOccurrences(of: "pass", with: "g") + String(mainInstallToken.suffix(5)) + "aISO")         // 获取 SIM/运营商 地区ISO
//: private let getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
private let data_tooId  = (String(dataCurfewTitle.suffix(9)) + String(const_canValue))     // 获取是否使用了代理
//: private let getMicStatus    = "getMicStatus"       // 获取麦克风权限
private let networkAlreadyKey    = (String(helperSecretMessage.suffix(5)) + String(notiAppealHourUrl.prefix(7)))       // 获取麦克风权限
//: private let getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
private let loggerAtFireServerSecret  = (String(constMapVersion) + kAllowHandleSecret.replacingOccurrences(of: "forward", with: "t"))     // 获取相册权限
//: private let getCameraStatus = "getCameraStatus"    // 获取相机权限
private let helperAdFormat = (String(modelErrorPhoneToken.prefix(8)) + String(data_applicationCenterPath.suffix(7)))    // 获取相机权限
//: private let reportAdjust    = "reportAdjust"       // 上报Adjust
private let k_installTitle    = (String(parserCaskMode))       // 上报Adjust
//: private let requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
private let notiEnsureDisableSecret = (const_mirrorTime.replacingOccurrences(of: "original", with: "r") + showPoorKey.lowercased() + "tLoca" + String(appSubUrl.suffix(5)))  // APP在后台收到音视频通话推送
//: private let getLangCode      = "getLangCode"        // 获取系统语言
private let data_deviceFireValue      = (String(k_behaviorMsg.prefix(6)) + String(managerPromptTitle.prefix(5)))        // 获取系统语言
//: private let getTimeZone      = "getTimeZone"        // 获取当前系统时区
private let noti_countermanString      = (String(showBringAppearSecret.prefix(6)) + String(mainNameBackgroundTitle.prefix(5)))        // 获取当前系统时区
//: private let getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
private let sessionEnsureStr = (String(managerSmartPath.suffix(4)) + "nstalledA" + String(cachePleaseMessage))   // 获取已安装应用列表
//: private let getSystemUUID    = "getSystemUUID"      // 获取系统UUID
private let user_micURL    = (String(serviceHourSubVersion) + String(data_endBackPath))      // 获取系统UUID
//: private let getCountryCode   = "getCountryCode"     // 获取当前系统地区
private let show_systemName   = (String(managerClickCount.prefix(7)) + String(app_byURL.prefix(7)))     // 获取当前系统地区
//: private let inAppRating      = "inAppRating"        // 应用内评分
private let factoryMilieuTime      = (String(mainBarrelDate.prefix(6)) + "atin" + networkAllowDict.replacingOccurrences(of: "forward", with: "g"))        // 应用内评分
//: private let apPay            = "apPay"              // 苹果支付
private let serviceMagnitudeeriorDict            = (String(helperFrameState.prefix(5)))              // 苹果支付
//: private let subscribe        = "subscribe"          // 苹果支付-订阅
private let main_fireStr        = (userTitleDocumentValue.replacingOccurrences(of: "poor", with: "r") + managerUnprocessedToken.replacingOccurrences(of: "display", with: "e"))          // 苹果支付-订阅
//: private let openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
private let k_alwaysAdjustError = (String(k_mirrorError.prefix(5)) + "yste" + String(constNetworkID.prefix(2)) + String(routerContentTitle)) // 调用系统浏览器打开url
//: private let closeWebview     = "closeWebview"       // 关闭当前webview
private let mainQualitySecret     = (String(dataCancelDocumentFormat))       // 关闭当前webview
//: private let openNewWebview   = "openNewWebview"     // 使用新webview打开url
private let show_kindMode   = (String(sessionHeadName.prefix(6)) + String(k_cutNamePath))     // 使用新webview打开url
//: private let reloadWebview    = "reloadWebview"      // 重载webView
private let mainDeviceUrl    = (dataTransformError.replacingOccurrences(of: "tool", with: "o") + "dWeb" + sessionPathResult.replacingOccurrences(of: "frame", with: "ie"))      // 重载webView
//: private let openSettings = "openSettings"                   // v2.0.4新增：打开通知设置页
private let data_todayList = (String(app_runKey) + String(formatterNativeYourUrl))                   // v2.0.4新增：打开通知设置页
//: private let getNotificationStatus = "getNotificationStatus" // v2.0.4新增：获取通知权限
private let appAbiesBracteataProductionFormat = (String(routerTempResult) + String(constTransportMessage)) // v2.0.4新增：获取通知权限
//: private let setScheduledLocalPush = "setScheduledLocalPush" // v2.0.5新增：设置定时本地消息推送
private let factoryTimeReplaceMicPath = (String(serviceColorDict) + String(app_workingPapersString)) // v2.0.5新增：设置定时本地消息推送

//: struct JSMessageModel: HandyJSON {
struct RevenueMemory: HandyJSON {
    //: var typeName = ""
    var typeName = ""
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: UnknownExclude?
    // 内购/订阅 H5参数
    //: var goodsId: String?     
    var goodsId: String?     // 商品Id
    //: var source: Int?         
    var source: Int?         // 充值来源
    //: var type: Int?           
    var type: Int?           // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?         
    var url: String?         // url
    //: var fullscreen: Int?     
    var fullscreen: Int?     // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?   
    var transparency: Int?   // transparency：0-webview白色背景 1-webview透明背景
    //: var time: [Int]?         
    var time: [Int]?         // 本地推送当天时间（24小时制）
    //: var msg: [String]?       
    var msg: [String]?       // 本地推送文案
}

//: struct UserInfoModel: HandyJSON {
struct UnknownExclude: HandyJSON {
    //: var headPic: String?   
    var headPic: String?   // 头像
    //: var nickname: String?  
    var nickname: String?  // 昵称
    //: var uid: String?       
    var uid: String?       // 用户Id
}

//: extension AppWebViewController {
extension DeliveryRefresh {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func openceOf(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = RevenueMemory.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case getDeviceID:
            case parserTooValue:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = DoCondition.extract()
                //: callBack(["typeName": model.typeName, "deviceID": adidStr])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: engineLoadGlobalMessage.map{scalePanel(pad: $0)}, encoding: .utf8)!: adidStr])

            //: case getFirebaseID:
            case mainModeMode:
                //: AppWebViewController.getFireBaseToken { str in
                DeliveryRefresh.failWith { str in
                    //: callBack(["typeName": model.typeName, "fireBaseID": str])
                    callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: networkFileValue.map{$0^41}, encoding: .utf8)!: str])
                }
                
            //: case getAreaISO:
            case appRaceID:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = DeliveryRefresh.today()
                //: callBack(["typeName": model.typeName, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: modelWorkerResult.reversed(), encoding: .utf8)!: arr.joined(separator: ",")])
                
            //: case getProxyStatus:
            case data_tooId:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = DeliveryRefresh.remote()
                //: callBack(["typeName": model.typeName, "isProxy": status])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: cacheHomePadDate.map{$0^167}, encoding: .utf8)!: status])
              
            //: case getLangCode:
            case data_deviceFireValue:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName, "langCode": langCode])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: showPackageProgressMsg.map{minimumLevel(interval: $0)}, encoding: .utf8)!: langCode])
                
            //: case getTimeZone:
            case noti_countermanString:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName, "timeZone": timeZone])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: show_enterDefiniteValue.map{$0^89}, encoding: .utf8)!: timeZone])
                
            //: case getInstalledApps:
            case sessionEnsureStr:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName, "installedApps": apps])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: kReceiptsVisibleDict.map{blackHourResult(succeed: $0)}, encoding: .utf8)!: apps])
                
            //: case getSystemUUID:
            case user_micURL:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName, "systemUUID": uuid])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: notiApplicationWaitId.map{nativeAccess(format: $0)}, encoding: .utf8)!: uuid])
                
            //: case getCountryCode:
            case show_systemName:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName, "countryCode": countryCode])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: formatterCurrencyNetID.map{$0^172}, encoding: .utf8)!: countryCode])
                
            //: case inAppRating:
            case factoryMilieuTime:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.requestInAppRating()
                DeliveryRefresh.item()

            //: case apPay:
            case serviceMagnitudeeriorDict:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.nut(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: k_acrossToken.map{noPass(race: $0)}, encoding: .utf8)!: success])
                    }
                }

            //: case subscribe:
            case main_fireStr:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.nut(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: k_acrossToken.map{noPass(race: $0)}, encoding: .utf8)!: success])
                    }
                }
                
            //: case openSystemBrowser:
            case k_alwaysAdjustError:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }
                
            //: case closeWebview:
            case mainQualitySecret:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName])
                //: self.closeWeb()
                self.intervalFilter()
                
            //: case openNewWebview:
            case show_kindMode:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                    //: let transparency = model.transparency,
                    let transparency = model.transparency,
                    //: let fullscreen = model.fullscreen {
                    let fullscreen = model.fullscreen {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    DeliveryRefresh.captureAfter(urlStr, transparency, fullscreen)
                }
                
            //: case reloadWebview:
            case mainDeviceUrl:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName])
                //: self.reloadWebView()
                self.recordRequest()
            
            //: case openSettings:
            case data_todayList:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName])
                //: if let url = URL(string: UIApplication.openSettingsURLString) {
                if let url = URL(string: UIApplication.openSettingsURLString) {
                    //: UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                    UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                }
                
            //: case setScheduledLocalPush:
            case factoryTimeReplaceMicPath:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName])
                //: LocalPushScheduler.shared.schedule(times: model.time ?? [], contents: model.msg ?? [])
                NowCharacter.shared.size(times: model.time ?? [], contents: model.msg ?? [])
                
            //: case getNotificationStatus:
            case appAbiesBracteataProductionFormat:
                //: AppPermissionTool.shared.requestNotificationPermission { auth, isFirst in
                UrlDoingly.shared.take { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: app_originTime.map{$0^158}, encoding: .utf8)!: auth, String(bytes: routerGatheringDate.map{thirdCancel(phone: $0)}, encoding: .utf8)!: isFirst])
                }
            
            //: case getMicStatus:
            case networkAlreadyKey:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                UrlDoingly.shared.third { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: app_originTime.map{$0^158}, encoding: .utf8)!: auth, String(bytes: routerGatheringDate.map{thirdCancel(phone: $0)}, encoding: .utf8)!: isFirst])
                }
                
            //: case getPhotoStatus:
            case loggerAtFireServerSecret:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                UrlDoingly.shared.blockIn { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: app_originTime.map{$0^158}, encoding: .utf8)!: auth, String(bytes: routerGatheringDate.map{thirdCancel(phone: $0)}, encoding: .utf8)!: isFirst])
                }
                
            //: case getCameraStatus:
            case helperAdFormat:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                UrlDoingly.shared.switchce { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName, String(bytes: app_originTime.map{$0^158}, encoding: .utf8)!: auth, String(bytes: routerGatheringDate.map{thirdCancel(phone: $0)}, encoding: .utf8)!: isFirst])
                }
                
            //: case reportAdjust:
            case k_installTitle:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        DoCondition.observer(token: token, count: count)
                    //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        DoCondition.object(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName])

            //: case requestLocalPush:
            case notiEnsureDisableSecret:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: appPathDecisionTooPath.map{behaviorPresent(device: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.pushLocalNotification(model)
                DeliveryRefresh.presentEvent(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event
//: extension AppWebViewController {
extension DeliveryRefresh {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func captureAfter(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = DeliveryRefresh()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        CommandConsent.that()?.present(vc, animated: true)
    }
    
    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func presentEvent(_ model: RevenueMemory) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()
                
            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(data_mapFormat)" + (String(viewDownTitle))
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: dataOrientList.map{requestTransform(ok: $0)}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }
                
            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }
    
    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func failWith(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
            //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func today() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func remote() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if DeliveryRefresh.used() || DeliveryRefresh.theory() {
            //: return true
            return true
        }
        //: return false
        return false
    }
    
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func used() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(const_labelContentID.uppercased() + String(mainHoiValue.prefix(6)))] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(viewTodayMessage.replacingOccurrences(of: "index", with: "T") + String(helperAtResult))] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(String(main_sLogToken) + String(controllerKindKey.suffix(5)))] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }
    
    /// 判断用户设备是否开启代理VPN
   /// - Returns: false: 未开启  true: 开启
   //: class func isUsedVPN() -> Bool {
   class func theory() -> Bool {
       //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
       guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
       //: guard let dict = proxy as? [String: Any] else { return false }
       guard let dict = proxy as? [String: Any] else { return false }
       //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
       guard let scopedDic = dict[(String(serviceLanguageCount) + formatterFormatServerError.replacingOccurrences(of: "to", with: "_"))] as? [String: Any] else { return false }
       //: for keyStr in scopedDic.keys {
       for keyStr in scopedDic.keys {
           //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
           if keyStr.contains((String(networkLevelValue))) || keyStr.contains((String(formatterActiveID))) || keyStr.contains((main_originalMessage.replacingOccurrences(of: "click", with: "ip"))) || keyStr.contains((data_panelMode.replacingOccurrences(of: "already", with: "p"))){
               //: return true
               return true
           }
       }
       //: return false
       return false
   }
    
    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func item() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
        //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }
    
    // MARK: - Event
    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func nut(productId: String, source: Int = -1, payType: AddressVariable, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        ContextSource.bringHome()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        CrabappleTail.shared.view(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            ContextSource.offHauled()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us")
                    ContextSource.firstAcross( (String(k_listChallengeMessage) + String(cacheSaveStatus.suffix(4)) + " Go to ") + "\"" + (String(user_applianceError) + constDomainMainValue.replacingOccurrences(of: "fail", with: "k")) + "\"" + (String(configBondRatingId) + String(helperImageProductID)))
                    
                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.pending()
                    
                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
}